# Copy this file to `.env` and fill in your API keys and endpoints
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-nano
CLAUDE_API_KEY=
OLLAMA_ENDPOINT=http://localhost:11434
# Comma-separated list of MCP server endpoints
MCP_SERVER_URLS=http://localhost:8001
# Timeout (seconds) for tool discovery
MCP_DISCOVERY_TIMEOUT=3
# Timeout (seconds) for tool execution
MCP_EXECUTION_TIMEOUT=10
